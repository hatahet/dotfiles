#!/bin/sh

iptables=/sbin/iptables
iptcmd="sudo $iptables"

[ -x $iptables ] || exit 1

$iptcmd -F
$iptcmd -P INPUT ACCEPT
$iptcmd -P OUTPUT ACCEPT

if [ -f $HOME/tmp/autossh.pid ]
then
    kill `cat $HOME/tmp/autossh.pid`
    rm $HOME/tmp/autossh.pid
fi
